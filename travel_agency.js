// create an array with different destinations for clients
let destinations = ["Bahamas", "Thailand", "Hawaii", "Italy", "California", "Toyoko", "Nigeria"];

console.log("Listed below are our destinations"); // Displays a message to the user

// Spaces are your friend, USE THEM
for (let i = 0; i < destinations.length; i++) {
    // USE TABS TO STURCTURE YOUR CODE
    console.log(destinations[i]); // Will ouput the first destination and then will continue to output each destination that follows associated with each index
}

// Be sure to use the variable name with brackets to specify which element by specifing the index