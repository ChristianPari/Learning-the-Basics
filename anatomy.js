// create array for body parts
let bodyParts = ["Hands", "Shoulders", "Knees", "Toes"];

console.log("What song uses these human body parts in a song?");

for (let i = 0; i < bodyParts.length; i++) {
    console.log(bodyParts[i]);
}